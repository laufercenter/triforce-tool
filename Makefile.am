AUTOMAKE_OPTIONS = subdir-objects
ACLOCAL_AMFLAGS = ${ACLOCAL_FLAGS}

bin_PROGRAMS = tft
tft_SOURCES = src/main.cc

AM_LDFLAGS = -ltriforce-0.39 -llapack -lblas -larmadillo

dist_noinst_SCRIPTS = autogen.sh


